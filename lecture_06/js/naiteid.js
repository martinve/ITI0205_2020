cars.forEach(function(car) { 
	console.log(car); 
});


cars.filter(function(car) { 
	return car != "BMW" 
});



let turn = {
	rerolls: 3,
	dice: [
		{
			value: null,
			locked: false
		}
	]
}



dice = dice.filter(die => die.locked.false);
dice.each(die => die.roll());

class Dice {

	roll() {
		if(this.locked) retu
	}

	draw() {
		//loop
			//kui locked == true, lisa klass locked
	}
}