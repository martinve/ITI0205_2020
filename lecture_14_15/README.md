# Nodejs client-server example

## Installing the backend

* `cd express-backend`
* Run `npm install` to install the dependencies
* Run `node index.js` to start the server

Run examples from `index.html` in browser.